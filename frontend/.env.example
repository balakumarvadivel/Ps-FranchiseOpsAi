# Leave unset for local dev — vite.config.js proxies /api to http://localhost:8000
# Set this for production if the frontend and backend are on different domains:
# VITE_API_BASE_URL=https://your-backend.onrender.com/api/v1
